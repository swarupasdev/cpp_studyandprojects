#pragma once
void Log(const char* message);
void initLog();
// as you can see I have created a header file which can include both functions 
//now we will  use it as #include "Log.h" in both cpp file to link these 2 programe